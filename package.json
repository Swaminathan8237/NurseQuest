{
  "name": "skillquest",
  "version": "1.0.0",
  "description": "SkillQuest — Learn · Practice · Excel | Interactive Quiz Platform",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently -n BE,FE -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && node server.js",
    "dev:frontend": "cd frontend && npx vite --port 5173",
    "build": "npm run install:all && cd frontend && npm run build",
    "start": "cd frontend && npm run build && cd ../backend && node server.js",
    "seed": "cd backend && node db/seed.js",
    "test:load": "npx playwright test tests/liveGameLoad130.spec.js --project=chromium --headed",
    "test:load:headless": "npx playwright test tests/liveGameLoad130.spec.js --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.1",
    "concurrently": "^9.1.2",
    "dotenv": "^17.4.2",
    "postgres": "^3.4.9",
    "socket.io-client": "^4.8.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
